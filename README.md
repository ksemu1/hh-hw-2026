# hh-hw-2026

## Задание

Доработать класс Switchboard таким образом, чтобы:
1. метод register_call принимал строку с информацией об активном звонке, разбивал её на части и создавал класс ActiveCall, перед этим создав классы LocalUser или ForeignUser для каждого участника звонка
2. метод get_active_calls_count возвращал количество активных звонков
3. метод get_cross_border_calls_count возвращал количетство активных звонков между LocalUser и ForeignUser

## Сдача ДЗ
1. Делайте fork репозитория себе на github (он должен быть публичный)
2. Клонируете, создаете ветку, пишите код
3. После написания кода, проверьте прохождение тестов
4. Для проверки создаете PR к себе в мастер, присылаете мне в личку. Я там оставляю комменты
5. После проверки при необходимости вносим правки отдельным коммитов в ту же ветку, пушим. Я смотрю ТОТ ЖЕ PR

### Условия выполнения задания:
1. Нельзя менять названия изначальных классов и методов, но можно создавать свои
2. Все ваши изменения должны быть только в файлах switchboard.py и test_switchboard.py (не все тесты прописаны)

### Критерии проверки

* Соблюдены условия выше
* Добавлены новые тесты лучше проверяющие функционал, старые тесты не были изменены. Все тесты успешно проходят.
* (***) Методы get_active_calls_count и get_cross_border_calls_count выполняются за константное время. Алгоритмическая сложность методов О(1)


## Описание данных

### raw_call

Параметр `raw_call` метода `register_call` предполагает строку следующего вида: "caller_id,caller_name,caller_phone,reciever_id,reciever_name,reciever_phone"

* caller_id - id позвонившего пользователя
* caller_name - имя позвонившего пользователя
* caller_phone - номер телефона позвонившего пользователя
* reciever_id - id ответившего пользователя
* reciever_name - имя ответившего пользователя
* reciever_phone - номер телефона ответившего пользователя

### Виды юзеров

Используйте LocalUser, если номер начинается с +7, ForeignUser для других номеров.


## Подготовка перед разработкой

* Создайте виртуальное окружение в директории - `python3 -m venv .env`
* Активируйте вирт окружение - `source .env/bin/activate`
* Устанавите зависимости из файла - `pip3 install -r requirements`
* Проверьте прохождение тестов - `pytest tests` (должно быть всё зеленым)
